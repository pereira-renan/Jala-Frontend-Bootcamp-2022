let backpack = ['banana', 'cellphone', 'something', 'swiss knife'];

// backpack.push('ighrughui45');
// backpack.pop();
JSON.stringify(backpack);
backpack[9] = 'Ok';

console.log(`let backpack: [${backpack.length}]`);

// for (let i = 0; i < backpack.length; i++) {}
